@using UI_Blazor.Client.Models

<div class="summary-cards-container">
    <div class="summary-card card-blue">
        <div class="card-icon">👤</div>
        <div class="card-details">
            <span class="card-label">Total Clientes</span>
            <span class="card-value">@Clientes.Count</span>
        </div>
    </div>

    <div class="summary-card card-green">
        <div class="card-icon">🏢</div>
        <div class="card-details">
            <span class="card-label">Empresas</span>
            <span class="card-value">@Clientes.Count(c => c.Tipo_Cliente == TipoCliente.EMPRESA)</span>
        </div>
    </div>

    <div class="summary-card card-purple">
        <div class="card-icon">👥</div>
        <div class="card-details">
            <span class="card-label">Personas</span>
            <span class="card-value">@Clientes.Count(c => c.Tipo_Cliente == TipoCliente.PERSONA)</span>
        </div>
    </div>

    <div class="summary-card card-orange">
        <div class="card-icon">🌍</div>
        <div class="card-details">
            <span class="card-label">Extranjeros</span>
            <span class="card-value">@Clientes.Count(c => c.Tipo_Cliente == TipoCliente.EXTRANJERO)</span>
        </div>
    </div>
</div>

@code {
    [Parameter]
    public List<Cliente> Clientes { get; set; } = new();
}